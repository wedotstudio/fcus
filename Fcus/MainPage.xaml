<Page
    x:Class="Fcus.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fcus"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button x:Name="HamburgerButton"
                    Width="50" Height="50" Background="Transparent" Click="HamburgerButton_Click">
            <Button.Content>
                <Image Source="Assets/Fcus-icon.png"/>
            </Button.Content>
        </Button>
        <SplitView Grid.Row="1" x:Name="splitView" DisplayMode="Overlay"  IsPaneOpen="False" 
              CompactPaneLength="50" OpenPaneLength="150" Background="White">
            <SplitView.Pane >
                <StackPanel Opacity="0.5">
                    <TextBlock Text="sth"/>
                    <Button Content="Bold" Click="Button_Click"/>
                </StackPanel>
            </SplitView.Pane>
            <SplitView.Content>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <RichEditBox x:Name="txt" Background="Transparent" BorderThickness="0" TextChanged="txt_TextChanged"/>
                    <Grid Grid.Row="1"  Margin="5,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Background="Transparent" BorderThickness="0">
                            <Button.Content>
                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE774;" Foreground="Gray"/>
                            </Button.Content>
                        </Button>
                        <TextBlock x:Name="ccount" Text="0 Character(s)"  Foreground="Gray" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                </Grid>
            </SplitView.Content>
        </SplitView>
        
    </Grid>
    
</Page>
