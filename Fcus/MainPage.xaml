<Page
    x:Class="Fcus.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Fcus"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="#f5f5f5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button x:Name="HamburgerButton"
                    Width="50" Height="50" Background="Transparent" Click="HamburgerButton_Click">
            <Button.Content>
                <Image Source="Assets/Fcus-icon.png"/>
            </Button.Content>
        </Button>
        <SplitView Grid.Row="1" x:Name="splitView" DisplayMode="Overlay"  IsPaneOpen="False" 
              CompactPaneLength="50" OpenPaneLength="150" Background="White">
            <SplitView.Pane >
                <StackPanel Opacity="0.5">
                    <AppBarButton x:Name="openFileButton" Icon="OpenFile" 
                   Click="OpenButton_Click" ToolTipService.ToolTip="Open file"/>
                    <AppBarButton Icon="Save" Click="SaveButton_Click" 
                   ToolTipService.ToolTip="Save file" 
                   RelativePanel.RightOf="openFileButton"/>
                </StackPanel>
            </SplitView.Pane>
            <SplitView.Content>
                <WebView x:Name="editor" Source="ms-appx-web:///index.html"/>
            </SplitView.Content>
        </SplitView>
    </Grid>
    
</Page>
